# bya-incomplete-web-app

## TODOs

The following features are missing and must be implemented before the app is fully functional:

- **TODO 1: Data persistence** — Tasks are currently stored only in memory and are lost on page refresh. Integrate `localStorage` (or a backend API) so tasks survive between sessions.
- **TODO 2: User authentication** — There is no login system. Add user accounts so each user sees only their own tasks.
- **TODO 3: Task editing** — Users cannot edit an existing task's text. Add an inline edit mode so task descriptions can be updated without deleting and re-adding them.
